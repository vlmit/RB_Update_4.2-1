<UserControl x:Class="Tessa.Extensions.Default.Client.Views.HelpViewExtensionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:tuic="Tessa.UI.Controls"
             d:DesignHeight="300"
             d:DesignWidth="300"
             AutomationProperties.AutomationId="ViewHelpViewExtension"
             mc:Ignorable="d">

    <Button Height="32"
            Margin="5,3"
            AutomationProperties.AutomationId="ViewHelpViewButton"
            Command="{Binding ShowHelpCommand,
                              Mode=OneTime}"
            FocusVisualStyle="{x:Null}"
            Style="{StaticResource BorderlessButton}"
            ToolTip="{Binding ToolTip,
                              Mode=OneTime}">
        <tuic:AutoDisabledPath Data="{StaticResource Thin228}"
                               Fill="Black"
                               Stretch="Uniform" />
    </Button>

</UserControl>
