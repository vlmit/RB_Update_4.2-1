#nullable enable

namespace Tessa.Extensions.Default.Server.PdfAnnotations.Types
{
    public enum AnnotationState
    {
        Stored = 1,
        Inserted = 2,
        Deleted = 3,
        Modified = 4
    }
}
